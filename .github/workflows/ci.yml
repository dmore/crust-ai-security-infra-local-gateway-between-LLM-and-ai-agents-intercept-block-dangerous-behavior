name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-go:
    name: Go Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v8

      - name: go fix (check for pending modernizations)
        run: |
          go fix ./...
          git diff --exit-code || { echo "go fix found pending modernizations — run 'go fix ./...' locally and commit the changes"; exit 1; }

      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

      - name: nilaway (nil safety)
        run: |
          go install go.uber.org/nilaway/cmd/nilaway@latest
          nilaway ./...

  security:
    name: Security Checks
    needs: [lint-go]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install gitleaks (DLP Tier 2 runtime dependency)
        run: go install github.com/zricethezav/gitleaks/v8@latest

      - name: Verify PIE binary
        run: |
          go build -buildmode=pie -o crust-pie .
          file crust-pie | grep -qi "pie\|shared object\|position independent"
          rm crust-pie

      - name: Fuzz tests (critical, 30s each — every PR)
        run: |
          go test ./internal/rules/ -fuzz=FuzzEngineBypass -fuzztime=30s -short
          go test ./internal/rules/ -fuzz=FuzzBuiltinRuleBypass -fuzztime=30s -short
          go test ./internal/rules/ -fuzz=FuzzNormalizeUnicode -fuzztime=30s -short
          go test ./internal/rules/ -fuzz=FuzzLoopbackRegex -fuzztime=30s -short
          go test ./internal/rules/ -fuzz=FuzzMCPToolBypass -fuzztime=30s -short
          go test ./internal/rules/ -fuzz=FuzzPipeBypass -fuzztime=30s -short
          go test ./internal/rules/ -fuzz=FuzzConfusableBypass -fuzztime=30s -short

      - name: Fuzz tests (full suite, 10s each — main only)
        if: github.ref == 'refs/heads/main'
        run: |
          go test ./internal/rules/ -fuzz=FuzzNormalizerBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzParseShellCommands -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzMatcherConsistency -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzExtractBashCommand -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzIsSuspiciousInput -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzContainsObfuscation -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzForkBombDetection -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzCommandRegexBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzHostRegexBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzJSONUnicodeEscapeBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzEvasionDetectionBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzGlobCommandBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzPipelineExtraction -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzContentConfusableBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzVariableExpansionEvasion -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzShapeDetectionBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzWebSearchURLBypass -fuzztime=10s -short
          go test ./internal/rules/ -fuzz=FuzzNormalAgentFalsePositive -fuzztime=10s -short
          go test ./internal/httpproxy/ -fuzz=FuzzParseSSEEventData -fuzztime=10s -short
          go test ./internal/httpproxy/ -fuzz=FuzzCopyHeaders -fuzztime=10s -short
          go test ./internal/httpproxy/ -fuzz=FuzzStripHopByHopHeaders -fuzztime=10s -short
          go test ./internal/httpproxy/ -fuzz=FuzzParseAnthropicEvent -fuzztime=10s -short
          go test ./internal/httpproxy/ -fuzz=FuzzParseOpenAIEvent -fuzztime=10s -short
          go test ./internal/httpproxy/ -fuzz=FuzzParseEvent -fuzztime=10s -short
          go test ./internal/httpproxy/ -fuzz=FuzzApplyResultToToolCalls -fuzztime=10s -short

  test:
    name: Go Test (${{ matrix.os }})
    needs: [lint-go]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install gitleaks (DLP Tier 2 runtime dependency)
        run: go install github.com/zricethezav/gitleaks/v8@latest

      - name: Build
        run: go build ./...

      - name: Build (notui)
        run: go build -tags notui ./...

      - name: Install PSScriptAnalyzer (Windows only)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser -ErrorAction Stop

      - name: Unit tests (with race detector and coverage)
        run: go test -race -short -coverprofile coverage.out -covermode atomic ./...

      - name: PSScriptAnalyzer — lint pwsh bootstrap script (Windows only)
        if: matrix.os == 'windows-latest'
        run: |
          go test -v -run 'TestPSScriptAnalyzer' ./internal/rules/...

      - name: PS worker fuzz tests (Windows only — pwsh is available on windows-latest)
        if: matrix.os == 'windows-latest'
        run: |
          go test ./internal/rules/ -fuzz=FuzzPSWorker_NoCrash -fuzztime=30s -short
          go test ./internal/rules/ -fuzz=FuzzExtractor_PSCommand -fuzztime=30s -short


  # macOS and FreeBSD full tests run only on main to reduce CI costs (macOS is 10x billing).
  # The jobs always complete (SUCCESS) so they can be listed as required status checks;
  # the expensive steps are skipped on non-main branches.
  test-expensive:
    name: Go Test (${{ matrix.os }})
    needs: [lint-go]
    runs-on: ${{ github.ref == 'refs/heads/main' && matrix.os || 'ubuntu-latest' }}
    strategy:
      matrix:
        os: [macos-latest]
    steps:
      - name: Skip on non-main branches
        if: github.ref != 'refs/heads/main'
        run: echo "macOS tests run on main only — skipping to reduce CI costs"

      - uses: actions/checkout@v4
        if: github.ref == 'refs/heads/main'

      - uses: actions/setup-go@v5
        if: github.ref == 'refs/heads/main'
        with:
          go-version-file: go.mod

      - name: Install gitleaks (DLP Tier 2 runtime dependency)
        if: github.ref == 'refs/heads/main'
        run: go install github.com/zricethezav/gitleaks/v8@latest

      - name: Build
        if: github.ref == 'refs/heads/main'
        run: go build ./...

      - name: Build (notui)
        if: github.ref == 'refs/heads/main'
        run: go build -tags notui ./...

      - name: Unit tests (with race detector)
        if: github.ref == 'refs/heads/main'
        run: go test -race -short ./...

  test-freebsd:
    name: Go Test (FreeBSD)
    runs-on: ubuntu-latest
    steps:
      - name: Skip on non-main branches
        if: github.ref != 'refs/heads/main'
        run: echo "FreeBSD tests run on main only — skipping to reduce CI costs"

      - uses: actions/checkout@v4
        if: github.ref == 'refs/heads/main'

      - uses: vmactions/freebsd-vm@v1
        if: github.ref == 'refs/heads/main'
        with:
          release: '15.0'
          usesh: true
          prepare: |
            pkg install -y go
          run: |
            go version
            go build ./...
            go build -tags notui ./...
            go test -short ./...

  e2e:
    name: E2E Tests (MCP stdio + HTTP + ACP)
    needs: [lint-go]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Pre-install MCP servers
        run: |
          npm install -g @modelcontextprotocol/server-filesystem
          npm install -g @modelcontextprotocol/server-everything

      - name: Install gitleaks (DLP Tier 2 runtime dependency)
        run: go install github.com/zricethezav/gitleaks/v8@latest

      - name: MCP E2E tests (stdio + HTTP)
        run: go test -race ./internal/mcpgateway/... -v -run E2E -timeout 180s

      - name: ACP E2E tests
        run: go test -race ./internal/acpwrap/... -v -run E2E -timeout 60s

  docker-test:
    name: Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build image
        run: docker build -t crust-test .

      - name: Start container and wait for healthy
        run: |
          docker run -d --name crust-ci -p 9090:9090 crust-test
          n=0; while [ "$n" -lt 15 ]; do
            curl -sf http://localhost:9090/health && exit 0
            sleep 1
            n=$((n + 1))
          done
          echo "Health check failed after 15 attempts"
          docker logs crust-ci
          exit 1

      - name: Graceful shutdown
        run: |
          docker stop --time 5 crust-ci
          EXIT_CODE=$(docker inspect crust-ci --format='{{.State.ExitCode}}')
          [ "$EXIT_CODE" = "0" ] || { echo "Exit code: $EXIT_CODE"; docker logs crust-ci; exit 1; }

      - name: Logs on failure
        if: failure()
        run: docker logs crust-ci 2>&1 || true
